user  nginx;
worker_processes  10;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    resolver 8.8.8.8;
    # resolver ${NAMESERVER};

    underscores_in_headers on;

    log_format upstream_logging '[$time_local] $remote_addr - $remote_user - $server_name to: $upstream_addr: $request upstream_response_time $upstream_response_time msec $msec request_time $request_time';
    # 上传文件最大为20MB
    client_max_body_size 20m;

    #服务路由websocket地址
    #upstream route_websocket_server {
    #   server 36.7.109.47:35366;
    #   keepalive 256;
    #}

    server {
        listen 80;
        listen 443 ssl;

       ssl_certificate /etc/nginx/https/server.crt;
       ssl_certificate_key /etc/nginx/https/server.key;

        ssl_session_cache    shared:SSL:1m;
        ssl_session_timeout  5m;

        ssl_ciphers  HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers  on;

        access_log /var/log/nginx/access.log upstream_logging;

        # /Files will go here TODO port
        location / {
            root /etc;
            index asriat.html;
        }

        #location ~ /iat/ {
        #  proxy_pass https://36.7.109.47:35366/iat/;
        #  proxy_http_version 1.1;
        #  proxy_set_header Upgrade $http_upgrade;
        #  proxy_set_header Connection "upgrade";
        #  proxy_connect_timeout 600000s; #配置点1
        #  proxy_read_timeout 600000s; #配置点2，如果没效，可以考虑这个时间配置长一点
        #  proxy_send_timeout 600000s; #配置点3
        #}
    }
}
